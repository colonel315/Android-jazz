<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">
    <TextView
        android:id="@+id/app_title"
        android:text="@string/app_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="25dp"
        android:gravity="center"
        android:textAlignment="center"
        android:textSize="30dp"/>

    <TextView
        android:id="@+id/num_players_string"
        android:text="@string/num_players_string"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/app_title"
        android:layout_marginTop="25dp"
        android:gravity="left"
        android:textSize="15dp"
        android:maxWidth="300dp" />

    <TextView
        android:id="@+id/list_of_players"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/num_players_string"
        android:layout_marginTop="25dp"
        android:gravity="left"
        android:textSize="15dp"
        android:maxWidth="300dp" />

    <Button
        android:id="@+id/go_to_add_player_intent_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/list_of_players"
        android:layout_marginTop="20dp"
        android:maxWidth="300dp"
        android:text="@string/add_player"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:background="@drawable/blue_action_button"
        android:gravity="center"
        android:onClick="goToAddPlayerActivity"/>

    <Button
        android:id="@+id/start_game_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/go_to_add_player_intent_button"
        android:layout_marginTop="20dp"
        android:maxWidth="300dp"
        android:text="@string/start_game_string"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:background="@drawable/orange_action_button"
        android:gravity="center"
        android:onClick="startGameButtonClick" />
</RelativeLayout>
